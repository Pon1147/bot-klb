(function () {
  'use strict';

  const CONFIG = {
    maxRetries: 1,
    delayBetweenCodesMs: 500,
    timeoutMs: 1500,
    pollMs: 50,
    submitClickRetries: 0,
    submitConfirmMs: 400,
    resumeKey: 'garena_redeem_v2_state',
  };

  window.Pon1147 = window.Pon1147 || {};
  window.Pon1147.config = CONFIG;
})();
