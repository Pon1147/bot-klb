PzDArbxYuL2f6RcaWE7T
GARENADF2503K8F3
GARENADFX9B7Q2
GARENADFQ1H8SZ
GARENADFM4Z8YJ
DFCantWait1114
GARENADFNY25010E034
AW88PTR3W9
AW88PTRP7D
DFIPPQOCSP
DFIPPQIASP
DFIPPQARVO
DFIPPQWLSP
DFIPPQTBAF
DFIPTFGJGFS
DFIPTFGASLGI
DFIPTFGZSKG
DFIZERODAMGS
DFIBRAKKESHMG
DFIBRAKKESHLEG
DFISPACECITYISG
DFISPACECITYMDB
DFISPACECITYECMO
  `
    .split('\n')
    .filter((c) => c.trim());

  const SOURCE = {
    name: 'Pon1147',
    discord: 'https://discord.gg/vz6w6c3Xe3',
  };
  // ==================== WATERMARK + SIGNATURE ====================
  const showSignature = () => {
    console.log(
      '%c🚀 Welcome to Pon1147 Redeem Tool %c| Made with ❤️ by Pon1147',
      'color:#4fc3f7;font-size:16px;font-weight:bold',
      'color:#66bb6a;font-size:13px',
    );

    console.log(
      '%cTham gia Discord: https://discord.gg/vz6w6c3Xe3',
      'color:#ffb74d;font-weight:bold',
    );

    // Thumbnail signature (ASCII art + emoji)
    console.log(
      `
    %c
    ██████╗  ██████╗ ███╗   ██╗
    ██╔══██╗██╔═══██╗████╗  ██║
    ██████╔╝██║   ██║██╔██╗ ██║
    ██╔═══╝ ██║   ██║██║╚██╗██║
    ██║     ╚██████╔╝██║ ╚████║
    ╚═╝      ╚═════╝ ╚═╝  ╚═══╝
    %cPon1147 Signature — Do Not Steal
  `,
      'color:#ff9800;font-family:monospace',
      'color:#ef5350;font-size:11px',
    );
  };

  // Chạy watermark ngay khi script load
  showSignature();

  // Optional: Inject vào DOM (nếu muốn hiện trên trang)
  const watermarkDiv = document.createElement('div');
  watermarkDiv.style.cssText = `
  position:fixed; bottom:10px; right:10px; 
  background:rgba(0,0,0,0.7); color:#4fc3f7; 
  padding:8px 12px; border-radius:6px; 
  font-size:12px; z-index:99999; pointer-events:none;
`;
  watermarkDiv.innerHTML = `Pon1147 Redeem Tool • <a href="${SOURCE.discord}" target="_blank" style="color:#66bb6a">Discord</a>`;
  document.body.appendChild(watermarkDiv);

  // ===== CONFIG =====
  const CONFIG = {
    maxRetries: 2,
    delayBetweenCodesMs: 1300,
    timeoutMs: 8500,
    submitConfirmMs: 400,
    resumeKey: 'garena_redeem_v2_state',
  };

  // ===== CLEANUP OLD =====
  document.getElementById('garena-redeem-summary-panel')?.remove();
  if (typeof window.__garenaRedeemRestoreHooks === 'function') {
    try {
      window.__garenaRedeemRestoreHooks();
    } catch (_) {}
  }

  // ===== UTILS =====
  const sleep = (ms) => new Promise((r) => setTimeout(r, ms));

  // Logging chi tiết với timestamp + color (từ current version)
  const log = (level, msg, extra = '') => {
    const ts = new Date().toLocaleTimeString();
    const color =
      {
        INFO: '#4fc3f7',
        SUCCESS: '#66bb6a',
        WARN: '#ffb74d',
        ERROR: '#ef5350',
      }[level] || '#fff';
    console.log(
      `%c[${ts}] [${level}]%c ${msg} ${extra}`,
      `color:${color};font-weight:bold`,
      'color:inherit',
    );
  };

  // ===== STATE =====
  let state = JSON.parse(localStorage.getItem(CONFIG.resumeKey) || 'null') || {
    index: 0,
    results: [],
    startedAt: Date.now(),
  };

  const saveState = () => localStorage.setItem(CONFIG.resumeKey, JSON.stringify(state));
  const clearState = () => localStorage.removeItem(CONFIG.resumeKey);

  // ==================== NETWORK MONITOR ====================
  // Architecture: currentAttempt (từ new version) — đúng cho sequential redeem
  const originalFetch = window.fetch?.bind(window);
  const originalXhrOpen = XMLHttpRequest.prototype.open;
  const originalXhrSend = XMLHttpRequest.prototype.send;

  let currentAttempt = null;

  // Kiểm tra response có phải redeem endpoint không (heuristic — có thể false positive nếu background API trả field 'code')
  const isRedeemResponse = (url = '', data) => {
    if (!data || typeof data !== 'object' || data.code === undefined) return false;
    const u = url.toLowerCase();
    return (
      /redeem|exchange|cdkey|gift|exc|code|verify/i.test(u) ||
      (/\/api\/v[0-9]/.test(u) && /code|redeem/.test(u))
    );
  };

  const createAttempt = () => ({
    startTime: Date.now(),
    done: false,
    resolve: null,
    promise: null,
  });

  const resolveIfMatch = (payload) => {
    if (!currentAttempt || currentAttempt.done) return;
    if (Date.now() - currentAttempt.startTime > CONFIG.timeoutMs) return;

    currentAttempt.done = true;
    currentAttempt.resolve(payload);
  };

  // Intercept fetch
  if (originalFetch) {
    window.fetch = async (...args) => {
      const url = typeof args[0] === 'string' ? args[0] : args[0]?.url || '';
      const res = await originalFetch(...args);
      res
        .clone()
        .json()
        .then((d) => {
          if (isRedeemResponse(url, d)) resolveIfMatch({ source: 'fetch', data: d, url });
        })
        .catch(() => {});
      return res;
    };
  }

  // Intercept XHR
  XMLHttpRequest.prototype.open = function (...a) {
    this.__url = a[1];
    return originalXhrOpen.apply(this, a);
  };

  XMLHttpRequest.prototype.send = function (...a) {
    this.addEventListener('loadend', () => {
      try {
        const d = JSON.parse(this.responseText);
        if (isRedeemResponse(this.__url, d))
          resolveIfMatch({ source: 'xhr', data: d, url: this.__url });
      } catch (e) {}
    });
    return originalXhrSend.apply(this, a);
  };

  window.__garenaRedeemRestoreHooks = () => {
    if (originalFetch) window.fetch = originalFetch;
    XMLHttpRequest.prototype.open = originalXhrOpen;
    XMLHttpRequest.prototype.send = originalXhrSend;
  };

  // ==================== DOM HELPERS ====================
  const visible = (el) =>
    el &&
    getComputedStyle(el).display !== 'none' &&
    getComputedStyle(el).visibility !== 'hidden' &&
    el.getBoundingClientRect().width > 0;

  // Button selectors rộng hơn (từ current version — tương thích tốt hơn)
  const selectors = {
    input: ['input.exc-input', 'input[placeholder*="code" i]'],
    button: ['a.btn-exchange', 'button.btn-exchange', 'button[type="submit"]', 'button', 'a'],
    dialog: ['[role="dialog"]', '.ant-modal', '.modal'],
    close: ['.ant-modal-close', '.close'],
  };

  const find = (sels, pred = () => true) => {
    for (const s of sels) {
      for (const el of document.querySelectorAll(s)) {
        if (visible(el) && pred(el)) return el;
      }
    }
    return null;
  };

  // Dialog observer
  let lastDialogText = '';
  const dialogObserver = new MutationObserver(() => {
    const d = find(selectors.dialog);
    if (d) lastDialogText = (d.textContent || '').trim() || lastDialogText;
  });
  dialogObserver.observe(document.body, { childList: true, subtree: true, characterData: true });

  // Chờ dialog hiện lên — polling (80ms) là cơ chế chính, observer chỉ cache lastDialogText làm fallback
  const waitForDialog = (timeout) =>
    new Promise((resolve) => {
      const start = Date.now();
      const check = () => {
        const d = find(selectors.dialog);
        if (d && d.textContent.trim()) resolve(d.textContent.trim());
        else if (Date.now() - start > timeout) resolve(lastDialogText || '');
        else setTimeout(check, 80);
      };
      check();
    });

  // ==================== CLASSIFIER ====================
  // Kết hợp statuses từ cả hai version (LOGIN_REQUIRED + LIMIT_REACHED từ current, MAINTENANCE từ new)
  const classify = (m) => {
    const t = String(m || '').toLowerCase();

    if (/^ok$|thành công|success/.test(t)) return 'SUCCESS';
    if (/error_hint_400067|reached the redemption limit|limit of cdkey group|đạt giới hạn/.test(t))
      return 'LIMIT_REACHED';
    if (/error_hint_400070|hết hạn|expired/.test(t)) return 'EXPIRED';
    if (/error_hint_400073|current cdkey present error/.test(t)) return 'PRESENT_ERROR';
    if (/error_hint_400054|không hợp lệ|invalid|sai|current cdk does not match/.test(t))
      return 'INVALID';
    if (/đã.*(nhận|sử dụng)|already|used/.test(t)) return 'USED';
    if (/captcha|xác minh|verification/.test(t)) return 'VERIFY';
    if (/lỗi mạng|network|rate|quá nhanh|too fast/.test(t)) return 'TEMP_ERROR';

    return 'UNKNOWN';
  };

  // ==================== REDEEM ONE ====================
  const redeemOne = async (code, idx, total) => {
    for (let att = 1; att <= CONFIG.maxRetries + 1; att++) {
      const input = find(selectors.input);
      const btn = find(selectors.button, (el) =>
        /đổi|redeem|exchange|confirm|xác nhận/i.test(el.textContent || ''),
      );
      if (!input || !btn)
        return { stt: `${idx}/${total}`, code, status: 'UNKNOWN', message: 'No UI' };

      // Set value programmatically
      Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value').set.call(input, code);
      input.dispatchEvent(new Event('input', { bubbles: true }));
      input.dispatchEvent(new Event('change', { bubbles: true }));

      await sleep(300);

      // Tạo attempt mới cho request này
      currentAttempt = createAttempt();
      currentAttempt.promise = new Promise((r) => {
        currentAttempt.resolve = r;
      });

      // Click và chờ kết quả
      btn.dispatchEvent(new MouseEvent('click', { bubbles: true }));
      await sleep(CONFIG.submitConfirmMs);

      // Promise.race: nhận result sớm nhất từ network HOẶC dialog (từ new version)
      const result = await Promise.race([
        currentAttempt.promise,
        waitForDialog(CONFIG.timeoutMs).then((text) => ({ dialog: text })),
      ]);

      const netData = result?.data || result;
      const dlg = result?.dialog || lastDialogText || '';
      const message = String(netData?.msg ?? netData ?? dlg ?? 'Timeout');
      const status = classify(message);

      // Retry nếu network error hoặc unknown
      if ((status === 'NETWORK_ERROR' || status === 'UNKNOWN') && att <= CONFIG.maxRetries) {
        log('WARN', `Retry ${att}/${CONFIG.maxRetries}`, code);
        currentAttempt = null;
        await sleep(1600);
        continue;
      }

      // Đóng dialog — dùng dispatchEvent thống nhất như button
      const closeEl = find(selectors.close);
      if (closeEl) closeEl.dispatchEvent(new MouseEvent('click', { bubbles: true }));
      await sleep(300);

      currentAttempt = null;
      return { stt: `${idx}/${total}`, code, status, message };
    }
  };

  // ==================== MAIN LOOP ====================
  log('INFO', '🚀 Redeem v2.7');

  try {
    for (let i = state.index; i < CODES.length; i++) {
      const res = await redeemOne(CODES[i], i + 1, CODES.length);
      state.results.push(res);
      state.index = i + 1;
      saveState();

      const level = res.status === 'SUCCESS' ? 'SUCCESS' : 'WARN';
      log(level, res.status, `${CODES[i]} | ${res.message}`);

      await sleep(CONFIG.delayBetweenCodesMs);
    }
  } finally {
    // Đảm bảo cleanup luôn chạy dù có crash (từ new version)
    console.table(
      state.results.reduce((a, r) => {
        a[r.status] = (a[r.status] || 0) + 1;
        return a;
      }, {}),
    );
    log('INFO', 'Hoàn tất', `${state.results.length} codes`);
    clearState();
    dialogObserver.disconnect();
    window.__garenaRedeemRestoreHooks?.();
  }
})();
